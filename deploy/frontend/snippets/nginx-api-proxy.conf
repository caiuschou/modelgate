# Paste INSIDE both HTTP and HTTPS server { ... } for modelgate.dev,
# BEFORE the SPA `location /` block (longer /api/ prefix wins when ordered first).
#
#   location /api/ {
#     proxy_pass http://127.0.0.1:8000;
#     ...
#   }

location /api/ {
  proxy_pass http://127.0.0.1:8000;
  proxy_http_version 1.1;
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;
  # WebSocket (e.g. /api/v1/ws/logs); required for Upgrade handshake behind nginx
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection "upgrade";
  proxy_buffering off;
  proxy_read_timeout 3600s;
  proxy_send_timeout 3600s;
}
